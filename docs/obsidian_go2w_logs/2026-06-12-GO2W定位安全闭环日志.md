---
created: 2026-06-12
updated: 2026-06-12
status: active
type: field-log
tags:
  - 机器狗/现场日志
  - 机器狗/GO2W
  - 机器狗/定位安全
  - 机器狗/XT16
---

# 2026-06-12 GO2W 定位安全闭环日志

## 本轮目标

收敛地图、重定位、XT16 安全、LLM 规划和 Operator UI，形成可重复、
可审计、默认不运动的验收闭环。

## 当前唯一架构口径

- 物理地图只有 `/home/unitree/test.pcd`。
- 逻辑地图 ID 为 `go2w_real_site`，后端历史 ID 为 `test`。
- `mapping_origin` 只有一个，表示建图坐标原点。
- 同一张 PCD 可以有多个重定位锚点；重定位锚点不等于导航拓扑点。
- LLM 只负责语义解析和任务编排，不直接控制底盘，也不能放宽安全门禁。
- 真实运动必须同时满足定位、地图身份、目标验证、XT16 标定与数据新鲜度、
  执行开关和 C++ Safety Gateway 授权。

## 已完成并有证据

- `mapping_origin` 重定位验证 5/5 通过。
- 每次连续 5 帧有效，位置误差约 `0.30 m`，yaw 误差约 `2.3-2.8°`。
- 无运动故障矩阵 5/5 通过，未知目标、错误锚点、缺少确认、XT16 未标定、
  未接线相对运动均未发送底盘命令。
- 本地 Python 全量测试 255/255 通过。
- 机器狗端 C++ 编译通过，6 个 smoke test 通过。
- 机器狗端关键 Python 测试 96/96 通过。
- Operator UI 已分离真实执行开关、安全导航许可和底盘运动状态，并显示
  XT16 标定、四向净空、任务队列和阻断原因。

关键 artifact：

```text
artifacts/field_acceptance/20260612_155522_no_motion_fault_matrix.json
```

## 2026-06-12 XT16 footprint 决策

根据 XT16 安装在机身中部的现场照片，四向 provisional footprint 暂时统一为：

```yaml
front: 0.30
rear: 0.30
left: 0.30
right: 0.30
filter_margin: 0.02
```

该值只是待测初值，不证明完整运动外廓位于 `0.60 x 0.60 m` 内。
标定状态继续保持 `pending_field_measurement`，真实导航继续失败关闭。

四处运行参数已统一：

- `configs/perception/xt16_geometry_calibration.json`
- `src/edge_autonomy/xt16_geometry.py`
- `scripts/xt16_lidar_geometry_summary.py`
- `scripts/go2w_xt16_geometry_sidecar.sh`

最终同步记录：

```text
git_commit: 1aeb105
local_tests: 255/255
robot_cpp_smoke: 6/6
robot_python_targeted: 96/96
dataset_validation: SFT 55/55, EVAL 55/55
robot_worktree: clean
runtime_footprint: front=0.30, rear=0.30, half_width=0.30
runtime_calibration: pending_field_measurement
runtime_safety: stale=uncalibrated_xt16_geometry, action=pause
```

当前停放场景的右侧净空约为 `0.001 m`，且左、右、后存在 low-hazard
近障提示，因此不能作为 XT16 空场 baseline。

## 本轮安全与规划修正

- 未定位或 SLAM 异常时，所有 prompt 模式统一转为 `safe_hold`。
- 未接线的 `mapless_scout` 和相对运动不能进入真实执行。
- 多目标只有出现明确顺序词时才建立串行队列，共享别名必须澄清。
- 未 `live_verified` 或带 `needs_calibration` 的拓扑点在 planner 层提前阻断。
- C++ LLM 在原始指令没有唯一命中 registry 时只能建议澄清，不能猜目标执行。
- UI 不再把“允许真实执行”误显示为“底盘正在运动”。

## 当前阻断项

1. XT16 五场人工标定尚未完成。
2. 未完成标定后的短距离直行、转弯、返回真实运动验收。
3. 未完成运动中定位丢失和传感器过期的自动暂停验收。
4. 拓扑点仍需逐点完成重复到位和姿态校准。

## 下一次现场唯一动作

不要先做导航。把机器狗停在左右和后方尽量大于 `0.6 m` 的位置，停稳 5 秒，
从机身最外缘用卷尺记录 `FRONT_M LEFT_M RIGHT_M REAR_M`，然后执行：

```bash
cd /home/unitree/Go2W_SLAM_AI
bash scripts/go2w_accept.sh xt16-baseline OPERATOR FRONT_M LEFT_M RIGHT_M REAR_M
```

基线通过后，再按前、左、右、后顺序放置一个 `0.6-1.5 m` 障碍物，每场执行：

```bash
bash scripts/go2w_accept.sh xt16-scene front OPERATOR MEASURED_M
bash scripts/go2w_accept.sh xt16-scene left OPERATOR MEASURED_M
bash scripts/go2w_accept.sh xt16-scene right OPERATOR MEASURED_M
bash scripts/go2w_accept.sh xt16-scene rear OPERATOR MEASURED_M
```

D435 只做前向交叉验证，不能替代 XT16 左、右、后净空。

## 长期 Agent 分工

- Jason：现场闭环、传感器、安全验收和材料证据。
- Godel：架构、LLM/UI、接口一致性和回归检查。
- 主线程：代码合并、实机操作、Git 同步和最终决策。

后续只复用这两个 agent，不再为同类小问题重复创建 agent。

## 后续记录规则

每次只追加以下信息：

```text
时间
git_commit / git_dirty
机器人位置与姿态
操作命令
是否允许真实执行
传感器与定位状态
artifact 路径
通过/失败结论
下一步唯一动作
```

不再把终端排查全过程写入日志；原始输出保存在 artifact。

## 2026-06-12 比赛安全链精简决策

用户确认移动 baseline 测试稍后进行，本轮不要求移动机器狗。

当前结论：此前安全机制存在重复裁决，Web、Python 和 C++ 兼容层都在重新解释
Gateway 已经给出的定位、点云、净空和置信度结论。比赛主路径收敛为：

```text
LLM / SemanticRouter
  -> TaskQueue
  -> Operator execution_enabled
  -> Python supervised executor
  -> SlamGateway target authorizer
  -> SlamGateway SafetySupervisor
  -> Unitree chassis
```

职责边界：

- LLM 只做意图、目标和任务拆解，不决定安全阈值。
- UI 的 `execution_enabled` 只表示人工允许尝试执行。
- D435 在 Web 层只显示前视深度诊断，不阻断 UI 解锁；Gateway 可在 XT16
  有效时保守融合前向深度，但 D435 不能单独授权导航。
- Python `gateway_safety.py` 只读取 Gateway 的 `allow_navigation/reason`。
- SlamGateway 是唯一运动安全权威，运行中 watchdog 负责暂停。
- XT16 五场标定、地图/PCD 哈希和 Git 绑定保留为赛前验收工具，不进入比赛主 UI。

本轮代码变化：

- 撤回尚未提交的 XT16 五场标定 Web/API 大面板。
- 删除 Web `/execute on` 对定位和双目的重复阻断。
- 删除 Python 对定位、XT16 来源、新鲜度、置信度和 `0.8 m` 净空的重复阈值。
- XT16 sidecar 默认使用 verified-record `auto` 模式。
- 标定 artifact 增加 `map_id`、registry SHA256 和 PCD SHA256。

验证：

```text
targeted_unittest: 85/85
operator_web_self_test: passed
movement_required: false
```

下一次现场动作仍是人工完成 XT16 baseline 和四向场景；在此之前继续保持
`pending_field_measurement`，不做真实导航验收。

## 单一安全权威版本验证

```text
implementation_commit: 8d23c4a
local_python: 251/251
operator_web_self_test: passed
architecture_html: desktop/mobile rendered, console clean
robot_python_targeted: 85/85
robot_cpp_build: passed
robot_cpp_ctest: 6/6
robot_gateway_build: passed
robot_gateway_ctest: 3/3
robot_worktree: clean
motion_commands_sent: false
```

本轮只完成代码、文档、编译和无运动回归，没有启动导航，也没有替代后续人工
baseline 与低速运动验收。
