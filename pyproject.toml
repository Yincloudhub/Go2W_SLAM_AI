[project]
name = "go2w-edge-autonomy"
version = "0.1.0"
description = "Edge autonomy scaffolding for GO2W semantic perception and weak-bandwidth remote interaction."
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
